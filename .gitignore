# Claude Code
.claude/local/
.claude/**/*.local.md
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node / Python (if we ever add tooling)
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
/build/
*.egg-info/

# `build/` (above) was previously unanchored and accidentally matched the
# tracked Drydock `commands/build/` directory. The leading slash now anchors
# it to the repo root only.

# Logs
*.log

# Env
.env
.env.local
